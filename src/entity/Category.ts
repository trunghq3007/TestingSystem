import { BsDatepickerConfig } from 'ngx-bootstrap/datepicker/public_api';

export class Category {
  id: number;
  categoryName: string;
  userCategory: Object;
  dateCreated: Date;
  status: number;
}
